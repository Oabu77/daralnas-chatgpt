apiVersion: v1
kind: Service
metadata:
  name: quranchain-service
spec:
  type: ClusterIP
  selector:
    app: quranchain
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
  type: LoadBalancer
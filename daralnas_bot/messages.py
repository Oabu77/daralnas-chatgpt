from __future__ import annotations

from textwrap import dedent


WELCOME = dedent(
    """
    Dar al-Nas on Telegram: education-first, human-reviewed, halal financial access.
    No keys are stored here. Actions requiring regulation are gated and reviewed.
    Use /ask for short guidance or tap a module to learn more.
    """
).strip()

DARALNAS = dedent(
    """
    Dar al-Nas offers jurisdiction-aware halal financing (e.g., diminishing musharakah),
    transparent service fees, and human approval. Pre-qualification is educational only.
    No promises, no riba, no speculative returns.
    """
).strip()

QURANCHAIN = dedent(
    """
    QuranChain anchors settlement, audit trails, and governance. Predictable gas fees
    reward infrastructure stewards; no speculative yields. It supports Dar al-Nas and
    keeps operations transparent.
    """
).strip()

MESHTALK = dedent(
    """
    Meshtalk OS powers proposals, deliberation, and AI summaries. Community discussion
    remains public; sensitive steps require human moderation. Education-first, no advice.
    """
).strip()

FUNGI = dedent(
    """
    Fungi Network visualizes long-term participation and trust. It is non-financial and
    resists fraud by highlighting credible contributors across Dar al-Nas and QuranChain.
    """
).strip()

DONATE = dedent(
    """
    Donations support audits, compliance, and community education. Funds are applied to
    transparency tooling, human moderation, and outreach. No hidden fees or investor-style
    promises.
    """
).strip()

AI_GUARDRAILS = dedent(
    """
    AI replies stay under 120 words, avoid rulings or advice, and default to education.
    For approvals, eligibility, or religious questions, we escalate to humans.
    """
).strip()
